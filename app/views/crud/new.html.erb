<div class="page__heading">
  <h1><%= t 'actions.new' %> <span class="text-muted"><%= object_title %></span></h1>
</div>

<div class="card card-form">
  <div class="row no-gutters">
    <div class="card-body">
      <%= simple_form_for(resource_class.new, local: true) do |f| %>
        <%= render 'form_wrapper', f: f %>
      <% end %>
    </div>
  </div>
</div>
